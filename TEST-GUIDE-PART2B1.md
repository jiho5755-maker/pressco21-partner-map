# Part 2B1 메이크샵 저장 실패 디버그 가이드

## 목적
makeshop-js-part2b1-no-inline.js 파일의 메이크샵 저장 실패 원인을 이진 탐색 방식으로 특정

## 파일 분할 전략

### 1단계: 2개로 분할
- ✅ `debug-part2b1-upper.js` (1-316줄) - 초기화, 로딩, 토스트, 파트너 리스트 상반부
- ✅ `debug-part2b1-lower.js` (317-632줄) - 모달, 즐겨찾기, 공유

### 2단계: 4개로 분할 (더 세밀한 테스트)
- ✅ `test-part2b1a-init-loading.js` (1-211줄) - 초기화 & 로딩
- ✅ `test-part2b1b-partner-list.js` (212-330줄) - 파트너 리스트
- ⚠️ `test-part2b1c-modal.js` (331-437줄) - **모달 (URL 포함)**
- ✅ `test-part2b1d-favorite-share.js` (438-632줄) - 즐겨찾기 & 공유

## 의심 구간

### Part 2B1c - 모달 (가장 의심됨)
**이유**: 외부 URL이 포함되어 있음

#### URL 포함 라인
```javascript
// 라인 61-63: 네이버 지도, 카카오맵 URL
'<a href="https://map.naver.com/v5/search/' + encodeURIComponent(partner.address) + '" ' +
'target="_blank" class="pm-nav-btn pm-nav-naver">🗺️ 네이버 지도</a>' +
'<a href="https://map.kakao.com/?q=' + encodeURIComponent(partner.address) + '" ' +
'target="_blank" class="pm-nav-btn pm-nav-kakao">🗺️ 카카오맵</a>' +

// 라인 82: 인스타그램 URL 생성
var instagramUrl = partner.instagram.startsWith('http') ? partner.instagram : 'https://instagram.com/' + partner.instagram;
```

**메이크샵 제약 가능성**:
1. `https://` 프로토콜이 보안 필터에 걸림
2. `encodeURIComponent()` 함수 사용이 문제
3. 조건부 연산자 (`? :`) + 문자열 연결 패턴
4. `.startsWith('http')` 메서드 사용

## 테스트 순서 (권장)

### 단계 1: 4개 파일 개별 테스트
메이크샵 관리자 > [디자인] > [스마트디자인 관리] > [JS] 탭에서 각 파일을 **순서대로** 저장 테스트

1. ✅ `test-part2b1a-init-loading.js` 저장 시도
   - 성공 → 초기화/로딩 코드는 문제 없음
   - 실패 → 이 파일 내부에 문제 있음 (추가 분할 필요)

2. ✅ `test-part2b1b-partner-list.js` 저장 시도
   - 성공 → 파트너 리스트 코드는 문제 없음
   - 실패 → HTML 문자열 연결이 문제일 수 있음

3. ⚠️ `test-part2b1c-modal.js` 저장 시도 (가장 의심됨)
   - 성공 → URL은 문제 없음 (Part 2B1d 테스트로)
   - 실패 → **이 파일이 원인!** (단계 2로 이동)

4. ✅ `test-part2b1d-favorite-share.js` 저장 시도
   - 성공 → 즐겨찾기/공유 코드는 문제 없음
   - 실패 → 이 파일 내부에 문제 있음

### 단계 2: Part 2B1c 세밀 분할 (문제 발견 시)

#### 옵션 A: URL 구간 격리
```javascript
// test-part2b1c-no-url.js (URL 제거 버전)
// 61-65번 라인 주석 처리
// 76-84번 라인 주석 처리
```

#### 옵션 B: URL을 변수로 분리
```javascript
// 하드코딩된 URL을 config로 이동
var naverMapUrl = 'https://map.naver.com/v5/search/';
var kakaoMapUrl = 'https://map.kakao.com/?q=';
var instagramBase = 'https://instagram.com/';
```

#### 옵션 C: URL 프로토콜 변경
```javascript
// https:// → //로 변경 (프로토콜 상대 URL)
'<a href="//map.naver.com/v5/search/' + encodeURIComponent(partner.address) + '" ' +
```

### 단계 3: 최소 재현 코드 생성
문제가 있는 라인만 포함한 최소 코드 작성

```javascript
// minimal-test.js
(function(window) {
    'use strict';

    // 문제가 되는 라인만 포함
    var testUrl = 'https://map.naver.com/v5/search/' + encodeURIComponent('서울시 강남구');

    console.log('테스트 성공:', testUrl);
})(window);
```

## 문제 패턴 분석 결과

### 검출된 패턴
1. ❌ **템플릿 리터럴** (`\${variable}`) - 없음
2. ✅ **외부 URL** (`https://`) - Part 2B1c에 3개
3. ✅ **encodeURIComponent** - Part 2B1c에 2개
4. ✅ **조건부 연산자 + URL** - Part 2B1c 82번 라인
5. ❌ **eval, new Function** - 없음
6. ❌ **특수 유니코드** - 없음

### 의심 순위
1. 🔴 **Part 2B1c (모달)** - URL + encodeURIComponent 조합
2. 🟡 **Part 2B1a (초기화)** - window.location 사용 (100번 라인)
3. 🟢 **Part 2B1b (파트너 리스트)** - 단순 HTML 문자열 연결
4. 🟢 **Part 2B1d (즐겨찾기/공유)** - 단순 로직

## 다음 단계

### 즉시 테스트
1. `test-part2b1c-modal.js`를 메이크샵에 저장 시도
   - **실패 예상** (URL 때문)

2. Part 2B1c에서 URL 구간만 주석 처리 후 재시도
   ```javascript
   // 라인 61-65 주석 처리
   // 라인 76-84 주석 처리
   ```

3. URL만 별도 파일로 분리
   - `test-url-only.js` (URL 생성 코드만)
   - 메이크샵 저장 가능 여부 확인

### 해결 방안 (문제 확인 후)
1. **URL 이스케이프 처리**
   ```javascript
   var naverUrl = 'https:' + '//map.naver.com/v5/search/';
   ```

2. **URL을 HTML에서 생성** (JS 대신)
   - 메이크샵 HTML 템플릿에 직접 작성

3. **URL을 외부 설정으로 분리**
   - Part 1 (CONFIG)에 URL 정의

4. **프로토콜 상대 URL 사용**
   ```javascript
   '//map.naver.com/v5/search/'  // https:// 제거
   ```

## 파일 정보

| 파일명 | 라인 수 | 크기 | 의심도 | URL 포함 |
|--------|---------|------|--------|----------|
| test-part2b1a-init-loading.js | 205 | 6.3K | 🟡 중간 | location만 |
| test-part2b1b-partner-list.js | 76 | 2.9K | 🟢 낮음 | 없음 |
| test-part2b1c-modal.js | 103 | 4.6K | 🔴 높음 | 3개 |
| test-part2b1d-favorite-share.js | 156 | 5.1K | 🟢 낮음 | 없음 |

## 보고 양식

테스트 후 아래 양식으로 결과 보고:

```
[Part 2B1 테스트 결과]

1. test-part2b1a-init-loading.js
   - 저장 결과: [성공/실패]
   - 에러 메시지: [있다면 기록]

2. test-part2b1b-partner-list.js
   - 저장 결과: [성공/실패]
   - 에러 메시지: [있다면 기록]

3. test-part2b1c-modal.js
   - 저장 결과: [성공/실패]
   - 에러 메시지: [있다면 기록]
   - URL 제거 후: [성공/실패]

4. test-part2b1d-favorite-share.js
   - 저장 결과: [성공/실패]
   - 에러 메시지: [있다면 기록]

[결론]
문제 파일: [파일명]
문제 라인: [라인 번호]
문제 패턴: [패턴 설명]
```

## 참고

- 원본 파일: `/Users/jangjiho/workspace/partner-map/makeshop-js-part2b1-no-inline.js`
- 전체 라인 수: 634줄
- 주요 기능: UI 컴포넌트 (토스트, 모달, 파트너 카드, 즐겨찾기, 공유)
